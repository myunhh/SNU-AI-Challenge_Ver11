# Batch 7 (inherited big-errors d>=4 + adjacent-swap samples)

## yx3vew (girl crossing monkey bars) — model FULL-REVERSAL (4-3-2-1 vs key 1-2-3-4)
Same domain as 8L7TfG, b44PjE. Model reversed traversal direction. Spotter/toddler move underneath across frames; direction genuinely hard from stills but model does a clean reverse. => PLAYGROUND-BAR TRAVERSAL DIRECTION is a systematic weakness.

## FOraJM (woman running down street) — d=5, model reversed approach/recede
Trailing-camera shot; runner apparent size changes monotonically. Key 2-1-4-3 = approaching; model 4-3-1-2 = receding. Direction-of-monotonic-change ambiguous but model picks opposite of audited key.

## lmOtxf (man+child tube over waterfall) — **CLEAN MODEL ERROR: ordered it UP the waterfall**
Frames: heh(upstream calm)→bnl(at top of drop)→bmw(in chute)→hdq(pool below). Key 4-2-3-1 = descent. Model 3-1-2-4 = hdq(pool)→bmw(chute)→bnl(top)→heh(upstream) = BOTTOM-TO-TOP, against gravity. Water physically falls DOWN; model reversed a gravity-obvious descent. Strong diagnostic: physical-causality/direction failure, NOT a coin flip, NOT key noise.

## uyWtdf (2-person contact-lens composite) — **model DEFENSIBLE; possible KEY ERROR**
Person B (black hair, flower-wall): fyv,oqq. Person A (brown hair, poster-wall): acz,tjs. Caption: "woman applies lens [B], THEN girl moves hand [A]" => B-clip then A-clip.
Model 4-1-2-3 => fyv,oqq (B) then tjs,acz (A) = correct 2-clip grouping matching caption.
Key 1-2-4-3 => acz(A),fyv(B),tjs(A),oqq(B) = INTERLEAVED, violates caption structure.
=> Model's clip-grouping is the caption-correct structure; key looks wrong. Counts against model unfairly. KEY-NOISE candidate.

## hHJkXc (2 gymnasts on beam) — CLEAN MODEL ERROR: broke subject grouping
Red-leotard frames ftu,tpo,wcj (3) then pink xll (1) per caption ("red rotates... then pink moves"). Key 3-2-1-4 keeps red(wcj,tpo,ftu)→pink(xll). Model 4-2-1-3 = wcj,tpo,xll(pink),ftu(red) — stuck a red frame AFTER pink, breaking grouping. Subject/scene-grouping failure (same mode as CbgVj3, NkMj3t, 0AezL1).

## 2Dhhti (dog: frisbee field + bath) — near-duplicate last-pair coin flip
Both agree field(cnq→ylo) first. Bath pair bsw(head-to-faucet)/zhs(sprayed): caption "moves toward spray, turns head to faucet" slightly favors model (zhs→bsw); key has bsw→zhs. Thin-margin near-duplicate; weak either way.

---
DIRECTION-FAILURE CLUSTER (highest-value real weakness): lmOtxf (waterfall, physics-clear MODEL ERROR), yx3vew/8L7TfG/b44PjE (monkey bars), FOraJM (runner). Single-subject monotonic-motion clips with few landmarks => model can't fix arrow-of-time, tends to reverse.
GROUPING-FAILURE CLUSTER: hHJkXc + (dpo) CbgVj3, NkMj3t, 0AezL1 — fails to keep same-scene frames contiguous in 2-clip composites.
KEY-NOISE in inherited sample: uyWtdf (defensible model), + both-wrong 8L7TfG/Z9CdGI/YZuSuG.
